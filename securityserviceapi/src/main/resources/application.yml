spring:
  datasource:
#    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB_NAME:security_jwt}
    #    username: ${MYSQL_USER:root}
    #    password: ${MYSQL_PASSWORD:21072003}
    url: jdbc:mysql://mysqldb:3306/security_jwt
    username: admin
    password: admin
#    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
